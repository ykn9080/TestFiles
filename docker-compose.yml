version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imctestform
    image: yknam/imctestform
    volumes:
      - ./src:/app/src
    ports:
      - "4009:80"
